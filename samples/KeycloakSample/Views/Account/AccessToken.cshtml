@model AccountViewModel

<pre><code class="language-json">@Html.Raw(Newtonsoft.Json.Linq.JValue.Parse(@Model.AccessTokenJson).ToString(Newtonsoft.Json.Formatting.Indented))</code></pre>

<pre><code>@Model.AccessToken</code></pre>

