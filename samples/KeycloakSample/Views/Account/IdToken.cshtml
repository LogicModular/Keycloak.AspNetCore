@model AccountViewModel

<pre><code class="language-json">@Html.Raw(Newtonsoft.Json.Linq.JValue.Parse(@Model.IdTokenJson).ToString(Newtonsoft.Json.Formatting.Indented))</code></pre>

<pre><code>@Model.IdToken</code></pre>

